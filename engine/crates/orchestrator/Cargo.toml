[package]
name = "orchestrator"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = {version = "0.13.3", features = ["blocking"]}
tokio = {version = "1.52.1", features = ["full"]}
chrono = "0.4.44"