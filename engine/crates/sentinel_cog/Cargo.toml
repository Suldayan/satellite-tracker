[package]
name = "sentinel_cog"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "gzip"] }
bytes = "1"
image = { version = "0.25", features = ["tiff"] }
anyhow = "1"
flate2 = "1"
thiserror  = "1"
log        = "0.4"